description "Locust server instance configured to serve locust slave"

start on runlevel [2345]
stop on runlevel [!2345]

setuid ubuntu
setgid ubuntu

chdir /home/ubuntu
exec locust --slave --master-host={{ hostvars['localhost'].locust_master_ec2.instances[0].private_ip_address }}
